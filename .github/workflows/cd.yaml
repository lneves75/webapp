name: CD

on:
  push:
    tags:
      - staging-*
      - uat-*
      - production-*

jobs:
  trigger-deployment:
    uses: lneves75/gh-workflows/.github/workflows/deployment.yaml@test-ci
    with:
      service: 'webapp'
      tag: ${{ github.ref_name }}
    secrets:
      token: ${{ secrets.GITOPS_TOKEN }}
