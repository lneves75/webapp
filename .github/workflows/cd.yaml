name: CD

on:
  push:
    tags:
      - staging-*
      - uat-*
      - production-*

jobs:
  trigger-deployment:
    uses: lneves75/gh-workflows/.github/workflows/deployment.yaml@3f5d6324ba1e0b0965745191baeaf88f99c862b9
    with:
      service: 'webapp'
      tag: ${{ github.ref_name }}
    secrets:
      token: ${{ secrets.GITOPS_TOKEN }}
